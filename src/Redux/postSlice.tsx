import { createSlice } from "@reduxjs/toolkit";

const initialState = [
    {}
]


const postSlice = createSlice({
    name: 'post',
    initialState,
    reducers: {}
})



export default postSlice.reducer;