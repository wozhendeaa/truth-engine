import { createSlice } from "@reduxjs/toolkit";

const initialState = [
    {}
]


const likeSlice = createSlice({
    name: 'like',
    initialState,
    reducers: {}
})



export default likeSlice.reducer;